<!DOCTYPE html>
<html>

<head>
    <title>Assign Task</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background: linear-gradient(135deg, #e0f7fa, #f3e5f5);
            min-height: 100vh;
        }

        .card {
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            border-radius: 1rem;
            margin-top: 30px;
            background-color: #ffffffd9;
            backdrop-filter: blur(4px);
            border: 1px solid #ddd;
        }

        .form-control:focus {
            border-color: #ab47bc;
            box-shadow: 0 0 0 0.2rem rgba(171, 71, 188, 0.25);
        }

        .btn-primary {
            background: #7e57c2;
            border: none;
            width: 25%;
        }

        .btn-primary:hover {
            background: #5e35b1;
        }

        h2 {
            color: #6a1b9a;
        }
    </style>
</head>

<body>

    <header>
        <%- include('header') %>
    </header>


    <div class="container">
        <div class="card p-5">
            <h2 class="mb-4 text-center text-primary">Assign Task to <%= emp.name %>
            </h2>
            <form action="/task/<%= emp.id %>" method="POST">
                <div class="mb-3">
                    <label class="form-label fw-semibold">Task Title</label>
                    <input type="text" name="title" class="form-control" placeholder="Enter task title..." required />
                </div>
                <div class="mb-3">
                    <label class="form-label fw-semibold">Task Description</label>
                    <textarea name="description" class="form-control" rows="4"
                        placeholder="Enter detailed description..." required></textarea>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary ">Assign Task</button>
                </div>
            </form>
        </div>
    </div>

    <!--  Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>